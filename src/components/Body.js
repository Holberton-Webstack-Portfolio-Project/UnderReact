import DropdownWidget from './DropdownWidget';

/**
 * The Body section of the app (inbetween the Nav and Footer)
 */
export default function Body() {
  return (
  <div class="container px-0 md:px-5 py-24 mt-20 mx-auto">
      <DropdownWidget />
  </div>
  )
}
